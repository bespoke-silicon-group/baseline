#ifndef __GAUSSIAN_ELIMINATION_HPP
#define __GAUSSIAN_ELIMINATION_HPP
#include <cstdint>

#define ABS(x) ((x) >= 0) ? (x) : (-(x))
#define MIN(x,y) (((x) <= (y)) ? (x) : (y))



#endif //__GAUSSIAN_ELIMINATION_HPP
