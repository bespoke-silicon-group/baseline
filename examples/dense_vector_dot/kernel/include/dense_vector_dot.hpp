#ifndef __DENSE_VECTOR_DOT__HPP
#define __DENSE_VECTOR_DOT_HPP
#include <cstdint>

#define ABS(x) ((x) >= 0) ? (x) : (-(x))
#define MIN(x,y) (((x) <= (y)) ? (x) : (y))



#endif //__DENSE_VECTOR_DOT_HPP
